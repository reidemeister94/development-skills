#!/usr/bin/env bash
# Re-anchor Claude Code implementation at the native Plan mode handoff.

set -euo pipefail

cat <<'JSON'
{
  "hookSpecificOutput": {
    "hookEventName": "PostToolUse",
    "additionalContext": "Native Plan mode is a permission surface, not the development workflow. The plan was approved; re-enter the Full path at Express. Create or update the repository plan and chronicle in completed tool calls before product or plugin edits; never combine them in one patch. Read the shared development loop, load the recorded standards, and set Current step to Implement. Then continue through Implement, Verify, Explain diff, and Review; plan work packages do not replace those gates."
  }
}
JSON
