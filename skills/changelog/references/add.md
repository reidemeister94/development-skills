# changelog · add

Insert one entry under `[Unreleased]`. Load `writing-guidelines.md`. Pre-flight + insertion mechanics: `SKILL.md` Steps 2–3.

1. **Entry.** If the user gave the text, use it. Else ask, via `AskUserQuestion`, the category (the six Keep a Changelog categories) + a ≤15-word imperative description.
2. **Review** the wording against `writing-guidelines.md`; if vague or too granular, propose a tightened version and confirm.
3. **Insert** via `SKILL.md` Step 3.
4. **Confirm:** print the path and the updated `[Unreleased]`. Hint: `/changelog add` again, or `/changelog release` to cut a version.
